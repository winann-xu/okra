# 秋葵 (Okra) 项目任务书

- 文档版本: v1.0
- 日期: 2026-09-17
- 状态: M0 进行中（2026-09-17 编码启动；Xcode 环境阻塞待用户处理，见 check point.md）
- 调用说明: 本文件是项目唯一权威任务书。任何后续 Agent 接手本项目，必须先读本文件，再读同目录 `check point.md`，遵守其中全部铁律与决策记录，再开始工作。

## 1. 项目基本信息

| 项 | 值 |
|---|---|
| 中文名 | 秋葵 |
| 英文名 | Okra |
| 定位 | macOS 独立 App：通过管理 /etc/hosts 优化中国大陆访问 GitHub，随系统启动，定时更新，实时显示访问状态 |
| 目标系统 | macOS 13+（依赖 SMAppService 登录项 API） |
| 技术栈 | Swift + SwiftUI（已确认），零第三方运行时依赖 |
| 项目目录 | /Users/winann/01-project/06-okra |
| 分发方式 | 非沙盒；Developer ID 签名 + 公证（自用可 ad-hoc + xattr 去隔离） |

## 2. 背景与问题

中国大陆访问 GitHub 存在 DNS 污染/限速，部分域名直连超时。2026-09-17 在本项目目标机器（未改 hosts）的直连基线实测：

| 域名 | 结果 |
|---|---|
| github.com | HTTP 200，耗时 10.0s（严重限速） |
| raw.githubusercontent.com | HTTP 301，0.27s（正常） |
| codeload.github.com | HTTP 301，0.45s（正常） |
| objects.githubusercontent.com | HTTP 000，10s 超时（不可达） |

结论：GitHub 属于 DNS 层问题，且存在中国网络可达的 IP（GitHub 在中国运营商网段持有 140.82.x.x 等地址，hellogithub 镜像即维护这些可达 IP），可通过 hosts 固定 IP 解决。

## 3. 可行性评估结论（2026-09-17 实测支撑）

1. 数据源可用：https://raw.hellogithub.com/hosts 实测 HTTP 200、0.16s、47 行（2498B），带 `# GitHub520 Host Start` 分隔标记，格式稳定可程序化解析。
2. 写 /etc/hosts 不被 SIP 阻止（SIP 保护 /System、/usr 等），root 权限可写。
3. 提权路径：GUI 无法静默提权 → 首次运行弹系统管理员授权对话框，一次性安装 root 级 launchd helper（Apple 推荐 SMJobBless 模式），此后更新全程静默。
4. 定时：launchd plist StartInterval=43200（12h），daemon 独立于 App 生命周期，App 崩溃/未登录时更新仍执行。
5. DNS 刷新：`sudo dscacheutil -flushcache; sudo killall -HUP mDNSResponder` 为标准有效操作。
6. 状态探测：每小时约 5~8 次 HTTPS 请求，开销可忽略。
7. 已知限制（诚实说明）：DNS 刷新只影响新解析请求，已建立长连接的浏览器页面需手动刷新；hosts 只固定 IPv4，若系统优先走 IPv6 个别域名可能不生效，探测逻辑需留意。

总体结论：完全可行，复杂度低-中，预计 1~3 天工作量。

## 4. 范围

v1 范围内（已确认，只做 GitHub）：
- GitHub 主域名及常用子域名的 hosts 管理
- 开机自启 + 12h 定时更新 + 60min 状态探测
- 菜单栏 UI、设置、一键还原、卸载

v1 范围外（明确不做，v2 另行立项评估）：
- HuggingFace（结论见 §8：主域名为 SNI 层封锁，hosts 方案原理上无效）
- 代理/TUN、App Store 上架、公开发布

## 5. 功能需求

FR1 开机自启
- 菜单栏 App 通过 SMAppService（macOS 13+）/ 登录项实现用户登录自启。
- root daemon 为系统级 launchd 服务，开机即运行，不依赖用户登录。

FR2 定时更新（12h）
- helper 拉取镜像源 → 解析 → 区块化更新 /etc/hosts（遵守 §7 铁律）→ 备份 → 刷新 DNS 缓存 → 写状态文件。
- 拉取失败/解析失败/校验失败：保留上一次可用配置，不清空、不半写，状态文件标记 error，UI 显示具体原因。
- 支持"立即更新"手动触发（UI 按钮 → 写 IPC 标记文件/触发信号，helper 立即执行）。

FR3 实时状态显示（探测周期 60min，已确认）
- 菜单栏图标：单色图标 + 状态染色（绿=畅通 / 黄=降级 / 红=不可达）。
- 判定规则：5 个探测域全部 2xx/3xx 且延迟 < 阈值 → 绿；部分异常 → 黄；全部失败 → 红。
- 探测域名：github.com、api.github.com、raw.githubusercontent.com、objects.githubusercontent.com、codeload.github.com。
- 探测方式：URLSession GET https://<domain>/，10s 超时，记录 HTTP 码 + 耗时。
- 每次 hosts 更新成功后立即触发一次探测（不等下个整点周期）。

FR4 UI（必须漂亮、人性化）
- 见 §6 设计规范。

FR5 设置与还原
- 更新周期（默认 12h，可调 4h/12h/24h）、探测周期（默认 60min，可调）。
- 一键还原：移除 Okra 区块，/etc/hosts 其余内容原样保留（§7 铁律验证）。
- 卸载：移除 helper、移除登录项、还原 hosts、清理状态文件与备份，干净退出。

FR6 轻量化
- 纯 Swift/SwiftUI，零第三方依赖；总包体 < 10MB；源码量级目标 ~2000 行。

## 6. UI 设计规范

菜单栏图标：SF Symbol 风格单色图标，按状态染色，异常时带轻微呼吸动效。

Popover 主面板（约 320pt 宽，原生材质 + 圆角 16 卡片，深色/浅色自适应，中文界面）：
- 顶部：大号状态灯 + 一句话结论（"GitHub 访问畅通" / "部分服务异常" / "GitHub 不可达"）。
- 中部：5 个域名列表，每行 = 域名 + 彩色状态点 + 延迟（如 230 ms）+ HTTP 码。
- 底部：最近 hosts 更新时间、最近探测时间、操作按钮 [立即更新] [立即探测] [设置]。

设置页：周期选择、镜像源展示（主源/备源状态）、[还原 hosts]、[卸载秋葵]、关于（版本 + 免责：仅个人用途，绕过网络限制属灰色地带，不公开分发）。

交互原则：不暴露任何命令行概念；所有危险操作（还原/卸载）二次确认；错误信息人话化。

## 7. 架构与技术决策

组件 A：Okra.app（菜单栏 SwiftUI App，非沙盒）
- 职责：UI 展示、60min 探测、设置读写、读状态文件、安装/卸载 helper 入口。

组件 B：OkraHelper（root 级 launchd daemon）
- 职责：拉取镜像、区块化更新 hosts、备份、刷 DNS、写状态文件、12h 定时。
- plist：/Library/LaunchDaemons/... StartInterval=43200 + RunAtLoad=true。

状态文件：~/Library/Application Support/Okra/status.json（helper 写、App 读）
```
{
  "last_update": "2026-09-17T12:00:00Z",
  "update_ok": true,
  "update_error": null,
  "source": "hellogithub",
  "entries": 47,
  "last_probe": "...",
  "probes": [{ "domain": "github.com", "http_code": 200, "ms": 230, "ok": true }],
  "overall": "green"
}
```
App 轮询该文件（或 FSEvents 监听），状态变化秒级反映到 UI。

hosts 修改铁律（用户明确要求，最高优先级，违反即验收不通过）：
1. 只管理 `# Okra Start` 与 `# Okra End` 之间的区块，任何情况下不读取-修改-写出区块之外的字节。
2. 首次写入前对 /etc/hosts 完整备份（带时间戳，存 App 支持目录）；还原 = 从当前文件移除 Okra 区块（保留他人后续改动），备份仅用于紧急恢复。
3. 区块化替换而非追加：重复更新只替换 Okra 区块内容，不产生重复条目。
4. 原子写：写临时文件 → 校验（Okra 区块正确 + 原有非 Okra 行全部逐行保留）→ rename 覆盖；任何一步失败则放弃写入。
5. 写前校验 /etc/hosts 可解析；镜像数据异常（空、无有效 IP 行、行数剧变超过阈值）拒绝写入。

更新镜像源（主备降级）：
- 主源（已验证 2026-09-17）：https://raw.hellogithub.com/hosts
- 备源 1（已验证 2026-09-17：200/0.26s/169 条，国内 CDN 快）：https://cdn.jsdelivr.net/gh/ittuann/GitHub-IP-hosts@main/hosts
- 备源 2（2026-09-17 已验证：200/0.64s/40 条；原 URL 仓库名笔误已修正，见 §11 D7）：https://raw.githubusercontent.com/521xueweihan/GitHub520/main/hosts
- 解析时归一化为 Okra 自有标记区块；源内原有 Start/End 标记丢弃，仅取 "IP + 域名" 行。

## 8. 重要排除项：HuggingFace 评估结论（v2 参考，勿在 v1 实现）

2026-09-17 实测证据链：
1. 无现成 HF hosts 镜像项目（GitHub520 仅维护 GitHub）。
2. DNS 污染：本机解析 huggingface.co = 31.13.71.19（Meta 段）；AliDNS = 31.13.81.4（Meta 段），污染确认。
3. 干净 IP 不可得：大陆网络下 8.8.8.8 / 1.1.1.1 的 DoH 传输层不可达，国内 DoH 均被污染。
4. SNI 层封锁（决定性）：用双解析器交叉验证的真实 CloudFront IP（13.33.183.46）钉住访问 huggingface.co → Connection reset by peer（0.07s 立即重置）。即干净 IP 也无法救回主域名。
5. 部分子域名可达：hf.co 钉 IP 后 HTTP 307（0.93s）；cas-bridge.xethub.hf.co 直连 HTTP 403（链路通）。cdn-lfs.huggingface.co 超时。
6. hf-mirror.com 国内镜像健康：HTTP 200，0.27s。

v2 建议方案（届时重新评估）：hosts 仅固定可达子域名（hf.co、cas-bridge.xethub.hf.co，干净 IP 需自建发布端：GitHub Actions 海外 runner 定时解析发布到 jsDelivr）+ 写环境变量 HF_ENDPOINT=https://hf-mirror.com 覆盖 CLI/huggingface_hub/Ollama 下载链路；浏览器场景只能引导直接访问镜像站（hosts 无法跨证书改写域名）。坚持直连真站需代理/TUN，不属本项目范畴。

## 9. 里程碑与验收标准

M0 项目骨架
- 目录结构、Xcode 工程（App target + helper target 或 shell helper 脚本）、launchd plist 模板。
- git 初始化（遵循目录内 AGENT.md 既定技术规范：项目一开始启用 git 及 GitHub，GitHub Flow，Conventional Commits）。
- 编码全程遵守 AGENT.md：简体中文沟通、函数 ≤50 行、单文件 ≤500 行、极简优先、精准修改、目标驱动执行。
- 验收：构建脚本（scripts/build.sh）产出可运行 Okra.app（D8 调整，不再要求 xcodebuild）；目录结构符合本任务书；git 已初始化、main 为默认分支且已推送 GitHub。

M1 root helper
- 实现 §7 全部 hosts 铁律 + 主备源拉取 + DNS 刷新 + 状态文件 + 定时。
- 验收：手动触发更新；diff /etc/hosts 证明仅 Okra 区块变化；连续更新两次无重复条目；镜像源模拟故障时保留旧配置且状态文件有 error；预置其他工具的 hosts 区块（含他人后续修改）验证 100% 保留。

M2 菜单栏 App
- 图标染色、popover 面板、60min 探测、更新后立即探测、状态文件读取。
- 验收：状态文件注入不同数据时 UI 秒级正确变色/刷新；实测 1 小时自动探测一次。

M3 设置/还原/卸载
- 验收：改周期生效；一键还原后其余区块逐字节不变；卸载后系统无残留（launchd 服务、登录项、状态文件）。

M4 打包与分发
- Developer ID 签名 + 公证（或自用 ad-hoc 路径）；首次运行安装流程（授权对话框装 helper）端到端验证；新 macOS 用户账号全流程走通。

每完成一个里程碑：更新 `check point.md`（状态表 + 日志追加），并保证 M 的验收证据（命令输出/截图）留档于项目 docs/ 目录。

## 10. 风险与缓解

| 风险 | 缓解 |
|---|---|
| hellogithub 服务停摆 | 三源降级 + 保留最后有效配置 + UI 明示错误 |
| 固定 IP 失效（GitHub 换 IP） | 12h 更新 + 60min 探测及时暴露降级 |
| macOS 版本差异 | 目标 macOS 13+，交付前在 14/15 各验证一轮 |
| Gatekeeper 拦截 | Developer ID + 公证；自用 ad-hoc + xattr -d com.apple.quarantine |
| 破坏用户其他 hosts 内容 | §7 铁律 + M1 专项验收（逐字节保留验证） |
| 政策风险 | 仅个人自用，不公开分发；关于页免责说明 |

## 11. 决策记录

- D1（2026-09-17，用户）：v1 只做 GitHub；HF 移至 v2。
- D2（2026-09-17，用户）：技术栈 Swift + SwiftUI。
- D3（2026-09-17，用户）：hosts 修改铁律——绝不影响其他程序写入的内容，只管理本项目区块（§7）。
- D4（2026-09-17，用户）：探测周期 60min，不更频繁。
- D5（2026-09-17，用户）：UI 必须漂亮、人性化。
- D6（2026-09-17，用户）：项目名 秋葵/Okra，目录 /Users/winann/01-project/06-okra。
- D7（2026-09-17，Agent 勘误）：§7 备源 2 原 URL 仓库名笔误（521xvwebsites/GitHub520 不存在，实测 404），修正为官方仓库 521xueweihan/GitHub520 并验证可达（200/0.64s/40 条）。备源 1（jsdelivr/ittuann/GitHub-IP-hosts）验证可达（200/0.26s/169 条）。
- D8（2026-09-17，用户）：构建体系改为 SwiftPM + 构建脚本（scripts/build.sh 组装 .app + ad-hoc 签名），不使用 Xcode（本机无 Xcode，且 2026 年起 Xcode 不再经 App Store 分发；参照 02-G_if/GPUMonitor 成熟做法，CLT 需固定 MacOSX26.5 SDK）。M0 验收相应调整为"构建脚本通过 + .app 可运行 + git/GitHub 就绪"，不再要求 xcodebuild。此前的 XcodeGen 方案（原 D8，Agent 提出）被本决策取代。
- D9（2026-09-17，Agent）：铁律 5"行数剧变超过阈值拒绝写入"仅在同源连续更新时生效（本次拉取 vs 上次该源写入的区块）；镜像源切换（主→备）不触发剧变比对。依据：三源条目数天然不同（当日实测 38/84/40 条），若跨源比对，主源停摆后备源（38→84，+121%）永远被拒，切换后系统永久锁死在错误态，与 §10"三源降级"缓解策略矛盾。同源剧变保护保留（防上游被篡改/损坏）。
- D10（2026-09-17，Agent）：原子写（临时文件→校验→覆盖）使用 rename(2) 系统调用而非 FileManager.moveItem——本机 macOS 27.0 实测 moveItem 在目标文件已存在时报 EEXIST（错误码 516）拒绝替换，裸 rename(2) 原子替换正常。实现细节，不影响接口。
- D11（2026-09-18，用户）：仓库 winann-xu/okra 保持 public（Agent 曾建议转 private 以贴合"仅个人自用、不公开分发"定位，用户明确选择保持公开）。澄清口径：任务书"不公开分发"指不发布 App 安装包/不上架 App Store，源码仓库公开不构成软件分发。

## 12. 附录：2026-09-17 实测原始数据

- hellogithub 主源：HTTP 200 / 0.160s / 2498B / 47 行，首行 "# GitHub520 Host Start"。
- GitHub 直连基线：github.com 200@10.0s；raw 301@0.27s；objects 000@10.0s；codeload 301@0.45s。
- HF：本地 dig huggingface.co=31.13.71.19；AliDNS DoH=31.13.81.4；hf.co(AliDNS)=44.212.132.255 等 AWS 段；8.8.8.8/1.1.1.1 DoH 空响应；--resolve 钉 31.13.71.19 → 12s 超时；钉 13.33.183.46 → RST；hf.co 钉 44.212.132.255 → 307@0.93s；cas-bridge.xethub.hf.co 直连 → 403@0.16s；hf-mirror.com → 200@0.27s。
